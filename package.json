{
  "name": "connect-lite",
  "private": true,
  "type": "module",
  "description": "Connect Lite is a static site — this package.json exists only so the screen check has a runner. Nothing here is deployed; GitHub Pages serves the HTML, JS and CSS and ignores the rest.",
  "scripts": {
    "check": "node check-screens.mjs"
  },
  "devDependencies": {
    "playwright": "^1.47.0"
  }
}
